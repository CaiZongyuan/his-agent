(function(){
const A=window.Academy,{D,S}=A;
function tab(page,def){return S.getUi(page).tab||def;}
function benchmark(){return D.healthAgentBench;}
function selectedCategory(){let id=S.getUi('healthagentbench').category||'meds';return benchmark().categories.find(x=>x.id===id)||benchmark().categories[0];}

function benchmarkHeroSide(){let s=benchmark().stats;return `<h3>首版基准规模</h3><div class="kpi-grid"><div class="kpi"><small>具体任务</small><strong>${s.tasks}</strong></div><div class="kpi"><small>任务大类</small><strong>${s.categories}</strong></div><div class="kpi"><small>参评智能体</small><strong>${s.agents}</strong></div><div class="kpi"><small>最佳成功率</small><strong>${s.bestSuccess}%</strong></div></div>`;}

function benchmarkOverview(){return `
<div class="metric-row">${A.metric('任务','54','覆盖患者旅程的多种工作')}${A.metric('大类','7','每类有独立环境')}${A.metric('重复试验','162','每个任务运行 3 次')}${A.metric('统一指标','成功 / 失败','严格任务成功率')}</div>
<div class="benchmark-media"><figure><img src="assets/healthagentbench-xray.png" alt="HealthAgentBench 使用的胸部 X 光示例"><figcaption>X 光报告纠错 · 二维影像与文本</figcaption></figure><figure><img src="assets/healthagentbench-ct.png" alt="HealthAgentBench 使用的 CT 切片示例"><figcaption>CT 异常分类 · 三维体数据</figcaption></figure><figure><img src="assets/healthagentbench-pathology.jpg" alt="HealthAgentBench 使用的病理图像示例"><figcaption>病理肿瘤区域选择 · 超大切片</figcaption></figure></div>
${A.section('它评测的不是“会不会答题”','每个任务都把智能体放进一个可以检查文件、查询数据、运行程序并提交产物的环境。',`<div class="benchmark-loop"><span>最少任务说明</span><b>→</b><span>原始医疗数据</span><b>→</b><span>可执行环境</span><b>→</b><span>自主规划与工具</span><b>→</b><span>提交产物</span><b>→</b><span>隐藏验证器</span></div>`)}
<div class="grid-3"><div class="card"><h4>真实数据形态</h4><p>关系数据库、临床文本、二维影像、三维 CT 和全切片病理图像，不会预先整理成一段提示词。</p></div><div class="card"><h4>长链路执行</h4><p>智能体必须先定位环境，再分解问题、调用工具、检查中间结果并修正方案。</p></div><div class="card"><h4>严格可验证</h4><p>每项任务都有专家标签、标准答案或性能门槛，最终统一成成功或失败。</p></div></div>
${A.learn('与医院智能体平台的关系','HealthAgentBench 给我们的不是七个可直接上线的产品，而是一套任务设计方法：真实环境、最少说明、完整轨迹、任务级验证器和统一成功标准。')}<div class="actions"><a class="btn primary" href="healthagentbench-labs.html">进入七类互动任务实验</a></div>`;}

function categoryCard(x){let active=selectedCategory().id===x.id;return `<button class="benchmark-category ${active?'active':''}" data-action="hab-category" data-category="${x.id}"><span class="category-count">${x.count}</span><span><strong>${x.name}</strong><small>${x.modality} · ${x.taskType}</small></span></button>`;}
function benchmarkCategories(){let c=selectedCategory();return `<div class="benchmark-catalog"><div class="category-list">${benchmark().categories.map(categoryCard).join('')}</div><div class="category-detail"><div class="eyebrow">${c.en}</div><h3>${c.name}</h3><div class="tags">${A.tag(c.modality,'blue')}${A.tag(`${c.count} 个任务`)}${A.tag(c.taskType,'orange')}</div><dl class="benchmark-definition"><dt>智能体面对的难点</dt><dd>${c.challenge}</dd><dt>怎样才算成功</dt><dd>${c.success}</dd></dl><div class="actions"><a class="btn primary" href="healthagentbench-labs.html?lab=${c.id}">进入本类互动实验</a>${c.id==='meds'?'<a class="btn" href="ehr-format-conversion.html">继续深挖真实配置</a>':''}</div></div></div>
${A.learn('为什么每类任务的成功标准不同？','报告纠错适合检查临床显著错误；试验匹配强调找全；CT 多标签分类要求全部标签正确；数据建模则与人工特征基线比较。底层指标不同，但最终都转换成一次任务是否真正完成。')}`;}

function benchmarkAnatomy(){let c=selectedCategory();let phases=[['任务说明','只说明目标和交付物，不替智能体拆步骤'],['环境','挂载原始数据、代码仓库、工具和可写输出目录'],['探索','智能体自行阅读、查询、编程、安装依赖或处理影像'],['产物','报告、分类、名单、数据文件或训练预测'],['验证','隐藏标签或参考产物进行任务专属检查'],['奖励','全部达到门槛记为 1，否则记为 0']];return `<div class="toolbar-row"><div class="filter-row">${benchmark().categories.map(x=>`<button class="filter-chip ${x.id===c.id?'active':''}" data-action="hab-category" data-category="${x.id}">${x.name}</button>`).join('')}</div></div><div class="task-anatomy">${phases.map((p,i)=>`<div class="anatomy-step"><span>${i+1}</span><div><strong>${p[0]}</strong><p>${p[1]}</p></div></div>`).join('')}</div><div class="grid-2"><div class="card"><h4>${c.name}如何落入这条链？</h4>${A.kv([['输入模态',c.modality],['任务形式',c.taskType],['具体任务数',String(c.count)],['主要难点',c.challenge]])}</div><div class="card"><h4>通过门槛</h4><p>${c.success}</p><div class="mini-callout">门槛刻意设置得接近完整、可靠的工作成果，而不是只奖励“部分看起来不错”。</div></div></div>`;}

function benchmarkInsights(){return `<div class="metric-row">${A.metric('最佳总体成功率','42%','Codex GPT-5.5')}${A.metric('影像任务平均','17%','跨所评 Codex / Claude Code')}${A.metric('文本任务平均','49%','同口径对比')}${A.metric('格式转换','9 / 10','参评智能体达到 100%')}</div><div class="grid-2"><div class="card"><h4>模型不是唯一变量</h4><p>同一个 GPT-5.5 在不同智能体运行框架中的成功率不同，说明工具暴露、上下文组织、恢复机制和执行策略都会影响结果。</p></div><div class="card"><h4>搜索空间会吞噬能力</h4><p>数据质量审计需要搜索 8 张、超过 80 万行的表。获得定位线索后明显改善，说明领域查询和患者摘要不是“锦上添花”。</p></div><div class="card"><h4>组合复杂度独立存在</h4><p>把多个错误类型放进一次任务，比拆成单项任务再汇总更难。工作分解和检查点设计是平台能力。</p></div><div class="card"><h4>影像仍是突出短板</h4><p>数百张 CT 切片和十亿像素病理切片要求智能体先建立分块、筛选和复核策略，不能把大对象直接塞入上下文。</p></div></div>${A.learn('读分数时要避免的误区','42% 不是医学正确率，而是 54 个严格任务中成功完成的比例。每类任务的样本量和通过门槛不同，所以类别分数更适合诊断能力缺口，总体成功率适合跟踪整体进展。')}`;}

function benchmarkMapping(){let rows=[['可执行任务环境','医院智能体命令工具（HACT）+ 智能体运行','生产环境还要加入数据授权、短期凭证和故障恢复'],['原始医疗数据','医院接入层 + 医院智能体临床模型（HACM）','保留来源版本、新鲜度和敏感等级'],['完整执行轨迹','智能体运行记录','加入证据、策略检查、人工批准和外部回执'],['隐藏验证器','场景包 + 验证器','与生产技能同版本，进入发布门禁和回归'],['二元任务奖励','技能发布是否达标','同时保留召回率、错误数和耗时等诊断指标'],['格式转换任务','接入映射实验 / 未来的接入助手','生成映射后必须跑样本、契约测试并由工程师批准'],['影像任务','资源引用 + 受控影像工具','原始敏感影像默认不进入云端模型上下文']];return `<table class="mapping-table"><thead><tr><th>HealthAgentBench</th><th>学院 / 平台中的对应物</th><th>生产化时必须补上</th></tr></thead><tbody>${rows.map(r=>`<tr><td><strong>${r[0]}</strong></td><td>${r[1]}</td><td>${r[2]}</td></tr>`).join('')}</tbody></table><div class="platform-bridge"><div><small>研究基准</small><strong>证明任务是否可被智能体完成</strong></div><b>→</b><div><small>模拟医院</small><strong>反复开发、诊断与回归</strong></div><b>→</b><div><small>生产平台</small><strong>授权、人工责任与持续运行</strong></div></div><div class="actions"><a class="btn primary" href="healthagentbench-labs.html">进入七类互动任务实验</a><a class="btn" href="ehr-format-conversion.html">深挖格式转换</a><a class="btn" href="critical-result.html">看生产式危急值闭环</a></div>`;}

A.registerPage('healthagentbench',()=>{let t=tab('healthagentbench','overview'),body={overview:benchmarkOverview,categories:benchmarkCategories,anatomy:benchmarkAnatomy,insights:benchmarkInsights,mapping:benchmarkMapping}[t]();return A.hero('HealthAgentBench · 医疗智能体评测基准','从“回答医学问题”走向“在环境中完成医疗工作”','这门课不复述论文摘要，而是把 HealthAgentBench 拆成任务、环境、轨迹、产物和验证器，再映射回医院智能体平台。',benchmarkHeroSide())+A.tabs([['overview','为什么需要它'],['categories','七类任务'],['anatomy','任务解剖'],['insights','结果启示'],['mapping','与平台融合']],t)+body;});
A.onAction('hab-category',el=>{S.setUi('healthagentbench',{category:el.dataset.category});A.refreshPage();});

function labConfig(){let existing=S.get().benchmarkConfig||{};return {defaultUntouched:true,copiedConfig:false,splitDemographics:false,omrPrefix:false,hospitalLabPrefix:false,icuChartPrefix:false,runtimeOverride:false,...existing};}
const configItems=[
  ['defaultUntouched','默认配置保持不变','不能直接修改 event_configs.yaml，也不能改变默认配置入口。'],
  ['copiedConfig','创建自定义配置','复制默认配置为 custom_event_configs.yaml。'],
  ['splitDemographics','拆分四类人口学事件','保险、语言、婚姻状态和种族改为入院时刻的独立事件。'],
  ['omrPrefix','区分门诊测量','门诊测量表（OMR）统一使用 OMR// 前缀。'],
  ['hospitalLabPrefix','区分医院检验','hosp/labevents 使用 HOSP_LAB// 前缀。'],
  ['icuChartPrefix','区分重症监护数据','重症监护病房（ICU）的 icu/chartevents 使用 ICU_CHARTEVENT// 前缀。'],
  ['runtimeOverride','本次运行显式使用新配置','只对这次运行传入自定义配置，不改变包的默认行为。']
];
function coreReady(c){return configItems.every(x=>c[x[0]]);}
function verifierChecks(c){let output=coreReady(c);return [
  {id:'default_config',name:'默认配置与默认入口未被破坏',ok:c.defaultUntouched},
  {id:'custom_config',name:'自定义配置存在且可以解析',ok:c.copiedConfig},
  {id:'demographics',name:'四类人口学字段成为入院时刻独立事件',ok:c.splitDemographics},
  {id:'prefixes',name:'门诊测量、医院检验与重症监护数据前缀正确',ok:c.omrPrefix&&c.hospitalLabPrefix&&c.icuChartPrefix},
  {id:'runtime',name:'流水线本次运行使用自定义配置',ok:c.runtimeOverride},
  {id:'files',name:'元数据与 train / tuning / held_out 文件完整',ok:output},
  {id:'content',name:'行数、结构和规范化内容哈希匹配标准答案',ok:output}
];}

function etlBrief(){return `<div class="metric-row">${A.metric('来源','MIMIC-IV 演示库','重症监护医学信息数据库第四版')}${A.metric('目标格式','MEDS','医疗事件数据标准')}${A.metric('任务数','1','完整流水线任务')}${A.metric('奖励','0 或 1','所有检查必须通过')}</div><div class="split"><div class="panel"><div class="panel-head"><h4>智能体可见</h4></div><div class="panel-body">${A.raw(`/workspace/MIMIC_IV_MEDS        真实转换代码仓库\n/workspace/staged_demo/raw_input   原始演示数据\n/workspace/output                  可写输出目录\n\n任务：复制并修改抽取配置，运行抽取、转换、加载（ETL），\n输出 /workspace/output/MEDS_cohort`)}</div></div><div class="panel"><div class="panel-head"><h4>仅验证器可见</h4></div><div class="panel-body">${A.kv([['标准摘要','gold_demo_summary.json'],['参考配置','reference_custom_event_configs.yaml'],['评分脚本','verify_output.py'],['最终结果','所有检查通过才获得 1']])}</div></div></div>${A.learn('两个数据名称分别是什么？','MIMIC-IV 是“重症监护医学信息数据库第四版”；MEDS 是“医疗事件数据标准”。这个任务把前者的表格数据整理成后者规定的患者事件序列。')}${A.learn('为什么这个任务很适合教学？','它同时检验仓库定向、配置理解、最小修改、依赖安装、命令执行、输出检查和防止投机取巧，而且结果能够用结构和内容哈希严格验证。')}`;}

function eventExample(c){let before=[['06:30','HOSPITAL_ADMISSION//EMERGENCY','insurance=Medicare；language=English；marital_status=Married；race=Asian']],after=c.splitDemographics?[['06:30','HOSPITAL_ADMISSION//EMERGENCY','不再携带四个人口学字段'],['06:30','INSURANCE//Medicare','独立事件'],['06:30','LANGUAGE//English','独立事件'],['06:30','MARITAL_STATUS//Married','独立事件'],['06:30','RACE//Asian','独立事件']]:before;return `<div class="split"><div class="panel"><div class="panel-head"><h4>默认抽取结果</h4></div><div class="panel-body">${eventTable(before)}</div></div><div class="panel"><div class="panel-head"><h4>当前自定义结果</h4>${c.splitDemographics?A.statusTag('已拆分'):A.statusTag('尚未拆分')}</div><div class="panel-body">${eventTable(after)}</div></div></div>`;}
function eventTable(rows){return `<div class="event-records">${rows.map(r=>`<div><time>${r[0]}</time><code>${r[1]}</code><small>${r[2]}</small></div>`).join('')}</div>`;}
function etlConfigure(){let c=labConfig();return `${eventExample(c)}<div class="config-lab">${configItems.map(x=>`<label class="config-check ${c[x[0]]?'checked':''}"><input type="checkbox" data-action="benchmark-toggle" data-key="${x[0]}" ${c[x[0]]?'checked':''}><span><strong>${x[1]}</strong><small>${x[2]}</small></span></label>`).join('')}</div><div class="actions"><button class="btn primary" data-action="benchmark-apply-reference">应用完整参考修改</button><button class="btn" data-action="benchmark-reset">重置本实验</button></div>`;}

function etlRun(){let c=labConfig(),run=S.get().benchmarkRun,checks=verifierChecks(c),ready=coreReady(c);let steps=[['检查代码仓库',true],['复制并修改配置',c.copiedConfig],['显式选择新配置',c.runtimeOverride],['运行转换流水线',!!run],['检查输出内容',!!run&&run.passed]];return `<div class="split"><div><div class="stepper">${steps.map((x,i)=>`<div class="step ${x[1]?'done':''}"><span class="step-num">${x[1]?'✓':i+1}</span><div><strong>${x[0]}</strong><small>${x[1]?'完成':'等待完成'}</small></div></div>`).join('')}</div><div class="actions"><button class="btn primary" data-action="benchmark-run">运行转换与隐藏验证器</button><button class="btn" data-action="benchmark-reset">重置</button></div></div><div class="verifier-console ${run?(run.passed?'passed':'failed'):''}"><div class="verifier-head"><span>HealthAgentBench 验证器</span>${run?A.statusTag(run.passed?'奖励 1.0':'奖励 0.0'):A.tag('尚未运行')}</div>${run?`<strong class="verifier-score">${run.passed?'通过':'失败'}</strong><p>${run.passed?'转换产物与标准答案一致。':'至少一项严格检查失败；部分完成不会获得任务奖励。'}</p><small>模拟耗时 ${run.duration} · ${run.time}</small>`:`<p>修改配置后运行。验证器会检查配置意图和最终 Parquet 数据文件内容。</p>`}</div></div>${!ready?A.learn('当前还不能通过','你仍然可以运行并查看具体失败项。评测的价值就在于把“差不多做完了”转换成明确、可诊断的失败。'):''}`;}

function etlVerifier(){let c=labConfig(),checks=verifierChecks(c),run=S.get().benchmarkRun;return `<div class="verifier-layout"><div class="check-list">${checks.map(x=>`<div class="verifier-check ${x.ok?'ok':'bad'}"><span>${x.ok?'✓':'×'}</span><div><strong>${x.name}</strong><small>${run?(x.ok?'通过':'失败'):'按当前配置预判'}</small></div></div>`).join('')}</div><div class="card"><h4>通过时应得到的目录</h4>${A.raw(`MEDS_cohort/\n├── metadata/\n│   ├── dataset.json\n│   ├── codes.parquet\n│   └── subject_splits.parquet\n└── data/\n    ├── train/0.parquet       804,944 行\n    ├── tuning/0.parquet       69,688 行\n    └── held_out/0.parquet     42,634 行`)}<div class="mini-callout">内容哈希前会按患者、时间和代码排序，所以无害的写入顺序差异不会导致失败。</div></div></div>${A.learn('验证器为什么既看配置又看结果？','只看配置容易接受“写法正确但没有真正运行”；只看输出又可能允许篡改默认入口或绕开要求。两层检查共同保证智能体完成的是指定工作。')}`;}

function etlPlatform(){let rows=[['真实转换仓库','医院接入适配器代码与映射配置'],['复制默认配置','从稳定模板创建医院专用映射'],['显式运行参数','连接实例或部署配置选择映射版本'],['标准输出目录','医院临床模型的资源、索引和来源信息'],['隐藏标准答案','脱敏样本夹具、契约测试和金标准摘要'],['二元奖励','接入版本能否进入发布或部署阶段'],['智能体解题轨迹','接入助手的建议、命令、差异和工程师审批记录']];return `<table><thead><tr><th>基准中的对象</th><th>生产平台中的对应设计</th></tr></thead><tbody>${rows.map(r=>`<tr><td><strong>${r[0]}</strong></td><td>${r[1]}</td></tr>`).join('')}</tbody></table><div class="platform-bridge"><div><small>输入</small><strong>接口文档 + 样本数据</strong></div><b>→</b><div><small>接入助手</small><strong>建议映射和转换配置</strong></div><b>→</b><div><small>验证</small><strong>样本 + 契约 + 来源检查</strong></div><b>→</b><div><small>责任</small><strong>接入工程师批准</strong></div></div>${A.learn('不能直接照搬的地方','HealthAgentBench 任务结束于生成正确文件；生产接入还要处理凭证、增量同步、来源版本、缓存新鲜度、长期运行、回滚和医院变更管理。')}`;}

A.registerPage('ehr-format-conversion',()=>{let t=tab('ehr-format-conversion','brief'),body={brief:etlBrief,configure:etlConfigure,run:etlRun,verifier:etlVerifier,platform:etlPlatform}[t]();return A.hero('HealthAgentBench 实验 01','电子健康记录（EHR）格式转换：亲手把“配置改对”变成“数据真的正确”','这个实验复现从重症监护医学信息数据库第四版（MIMIC-IV）到医疗事件数据标准（MEDS）的任务结构。你可以遗漏某项修改、污染默认配置，或应用完整方案，再观察严格验证器如何判定。')+A.tabs([['brief','任务说明'],['configure','修改配置'],['run','运行流水线'],['verifier','验证器'],['platform','映射到平台']],t)+body;});
A.onAction('benchmark-toggle',el=>{let key=el.dataset.key;S.mutate(st=>{st.benchmarkConfig=st.benchmarkConfig||labConfig();st.benchmarkConfig[key]=!!el.checked;st.benchmarkRun=null;});S.log('benchmark.config_changed',`${key}=${!!el.checked}`,null,'HealthAgentBench Lab');A.refreshPage();});
A.onAction('benchmark-apply-reference',()=>{S.set({benchmarkConfig:{defaultUntouched:true,copiedConfig:true,splitDemographics:true,omrPrefix:true,hospitalLabPrefix:true,icuChartPrefix:true,runtimeOverride:true},benchmarkRun:null});S.log('benchmark.reference_applied','应用完整自定义抽取配置',null,'HealthAgentBench Lab');A.toast('参考修改已应用，可以运行验证器','good');A.refreshPage();});
A.onAction('benchmark-reset',()=>{S.set({benchmarkConfig:{defaultUntouched:true,copiedConfig:false,splitDemographics:false,omrPrefix:false,hospitalLabPrefix:false,icuChartPrefix:false,runtimeOverride:false},benchmarkRun:null});S.log('benchmark.lab_reset','EHR 格式转换实验已重置',null,'HealthAgentBench Lab');A.refreshPage();});
A.onAction('benchmark-run',()=>{let c=labConfig(),checks=verifierChecks(c),passed=checks.every(x=>x.ok),run={passed,checks,time:S.get().simTime,duration:passed?'4 分 12 秒':'1 分 48 秒'};S.set({benchmarkRun:run});S.log('benchmark.verifier_completed',`EHR Format Conversion ${passed?'PASSED reward=1':'FAILED reward=0'}`,null,'HealthAgentBench Lab',passed?'info':'danger');A.toast(passed?'任务通过：奖励 1.0':'任务失败：查看验证器明细',passed?'good':'bad');A.refreshPage();});
})();
