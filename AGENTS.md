# 医疗智能体系统

## 项目

本仓库用于医疗智能体（Medical AI Agent）系统的探索。核心目标是：**探索医疗领域与 Agent 结合的场景，研究如何提高不同场景下的智能体能力，并产出可运行的 demo 与案例**（参考项目源码放 `references/`，如 `references/DSH-AGUI-demo`）。围绕这一目标推进三类工作：

1. **场景探索与 demo**：针对具体医疗场景分析智能体的任务、数据和工具边界，做 demo 和案例验证，沉淀提升该场景 agent 能力的方法。
2. **学习与教学**：帮助用户学习和理解相关论文与开源项目。参考论文转成 Markdown 保存到 `docs/papers/`，学习产出的教程、讲解与练习保存到 `docs/lessons/`。
3. **实验**：在理解的基础上做实验。数据放 `data/`，模型放 `models/`，实验与生成的 PDF、视频等产物放 `outputs/`。

- 科研检索遵循下文的分层流程；接口合同见 `docs/Infra-Agent-Research-Search-Guide.md`。
- 平台与产品设计沉淀在 `docs/platform/`，业务语言以根目录 `CONTEXT.md` 术语表为准。
- 明确区分已验证事实、论文结论、工程假设和待验证猜想，并保留来源与不确定性。

## 目录约定

提交到 Git 的内容：

- `docs/papers/`：参考论文的 Markdown 阅读版与 `INDEX.md` 索引；只提交 Markdown 和正文引用的图片。
- `docs/lessons/`：帮助用户学习论文和项目产出的内容（讲解、教程、图解、练习）。
- `docs/research/`：调研笔记与实验记录。
- `docs/platform/`：医疗智能体平台设计文档。
- `docs/style/`：可复用视觉素材（参考图、图稿源、prompt）；供正文消费的最终图片放对应文档的 `assets/` 目录。
- `demos/`：面向具体医疗场景的自研 demo 与案例项目；只提交源码与文档，大型依赖、数据和产物仍放 Git 忽略目录。
- `scripts/`：可复用工具脚本；`notebook/`：实验 notebook。

Git 忽略、仅保存在本地的大文件目录：

- `references/`：克隆的参考项目源码。只读参考，不修改上游代码；引用其实现时写明项目与文件路径，不把上游代码复制进本仓库主体。
- `data/`：数据集与实验数据。入库前在 `docs/research/` 或实验记录中写明来源、许可和获取方式。
- `models/`：模型权重与 checkpoint。记录来源、版本和校验和。
- `outputs/`：实验与生成产物（PDF、视频、图片母版、候选输出等）。需要版本管理的成品整理后移入 `docs/` 相应目录，不在 `outputs/` 中直接提交。

## 科研检索

先查 `docs/papers/INDEX.md`，复用已评估材料并识别缺口；再把问题改写为包含医疗场景、智能体任务、数据或环境和方法的高信号英文查询。每个检索任务先执行一个查询，只在结果明显缺失时精炼一次，并记录来源、查询词、调用次数、失败和未解决缺口。

### 来源路由

1. **默认发现：**使用 `$infra-scholar-search` 直接调用 Scholar HTTP API，先看前 10 条的题名、DOI、摘要、版本和来源。它是本项目日常主题检索及题名/DOI 查找的第一入口；保持顺序调用，最多不超过已验证的 QPS 3。
2. **证据与结构补全：**需要自然语言证据片段、可定位全文、图表或相关工作时使用 `$sciverse`；需要年份、作者、期刊、OA、引用量等硬筛选时先 `meta-search`，再把候选 `doc_id` 作为硬范围做语义检索。元数据结果按 DOI 和版本去重，不能把预印本与正式版当成两项独立证据。
3. **ML 方法精读：**只对医疗智能体方法、LLM/Agent 模型组件和 benchmark 使用 `$sciverse-paper-schema`。其语料主要是已解析的 AI 会议论文；空结果仅表示该语料未收录，不能推出相关文献中不存在。
4. **覆盖审计：**用户要求系统综述、跨库高召回、MeSH、严格引文核对或引用管理时使用 `$nature-academic-search`，按 PubMed/CrossRef/arXiv 的 T1 来源开始，再按需扩展 Semantic Scholar、bioRxiv/medRxiv。MCP 不可用时的 OpenAlex fallback 只作补充召回，不替代 PubMed 或原文核验。
5. **窄源工具：**OpenCLI arXiv 仅用于明确的 arXiv 题名、ID、作者或预印本查询；Semantic Scholar 仅在需要引文图或相关推荐且 API 当前可用时启用。`$nature-literature-pipeline` 只用于用户明确要求的定时文献监控或推送，不用于一次性交互检索。

### 收敛与核验

1. 合并结果时先规范化 DOI；无 DOI 时用规范化题名和第一作者识别重复。合并预印本、会议版和正式出版版，保留版本关系，并优先采用元数据更完整的正式来源。
2. 搜索返回的标题、snippet、摘要、引用数和模型生成总结都只是候选线索。影响项目决策的机制、方法、数据、指标和数值必须回到 DOI/出版页、预印本原文、数据集或官方代码核验；SciVerse 证据同时保留 `doc_id`、offset/页码和题名。
3. 一次精炼后若不再出现新的高优先级方法，且关键结论已有一手来源，即停止扩搜。若来源失败或覆盖不足，报告已检索范围和缺口；“未命中”不得表述为“研究不存在”。
4. 完成标准：所有实际用于判断的候选均在 `docs/papers/INDEX.md` 中标为采用、备选或排除；检索台账完整；关键结论均有可复核的一手来源。未查看题名或摘要的原始命中不必逐条入索引。

## 协作对象

用户熟悉软件开发、AI 和 Agent，但临床医学与医疗信息化背景有限。医学或领域知识会影响理解或决策时，主动教学：

1. 首次出现的术语给出中文、英文或缩写及一句话定义。
2. 按“直觉解释 -> 领域机制 -> 与当前论文或项目的关系 -> 对数据或模型的影响”展开；只讲当前问题需要的深度。
3. 可借用软件或机器学习类比，但要标明类比的边界，不能把类比当作医学机制。
4. 对电子病历（EHR）、临床工作流、医学推理与诊断决策、临床指南与循证医学、临床研究设计、医疗智能体评估基准等基础概念不默认用户已经掌握。
5. 发现用户的医学或领域前提有误时，先纠正并解释原因，再继续设计或实现。

### 教学模式

- 进入系统学习或教程共创时，先完整讲透一个因果单元，再检验理解。每个单元依次覆盖直觉解释、领域机制、论文或系统流程中每一步怎样实现及为何需要、主要失败与噪声来源、与探索任务的关系、对数据或模型的影响。
- 对问诊、检查检验、诊断、治疗、随访等临床环节，以及数据抽取、清洗、标注、评估等实验环节，不把过程名当作解释；说明输入、发生的操作、输出，以及该步骤解决的问题。遇到用户追问时继续向下展开必要机制，不用术语替换机制。
- 每个完整单元结束后至多提出一个高价值诊断题；用户表示解释不足时，先补齐缺失链路，再继续提问。只有用户明确要求密集 grilling 时才增加问题数量。
- 软件和机器学习类比只用于建立直觉，并紧邻说明类比边界。把互动中暴露的理解缺口转写为 `docs/lessons/` 教程的解释顺序、常见误区和练习，不保存聊天式转录。

## 论文与文档

- `docs/papers/` 保存与项目高度相关、可复核的论文阅读材料；`INDEX.md` 中必须保留权威原文 URL/DOI。论文 PDF 仅作本地核验输入，不提交到 Git；Markdown 是机器转换的阅读版，摘要、转述或翻译须标明性质，不能冒充原文。逐字引用、公式和数据核验以 PDF 或出版方原文为准。
- 对公开论文 PDF，运行 `scripts/mineru-pdf-to-markdown.sh INPUT.pdf [OUTPUT_DIR]` 使用 MinerU 生成带图片的 Markdown 阅读版，整理后保存到 `docs/papers/`。MinerU 请求必须在进程级禁用代理并绕过 WSL Fake-IP DNS，不修改系统全局代理。
- 每次检索、阅读或新增论文后，同步更新 `docs/papers/INDEX.md`。至少记录规范标题、摘要、原文链接/DOI、本地文件、与项目的核心关联、相关论文及关系、关键词、版本或年份、检索日期；缺失项明确标为待核验。
- 索引用于记录所有已评估的候选论文，不只记录最终采用的论文；说明采用、备选或排除及理由，避免重复检索。
- MinerU 和后续外部 API 的密钥、Token 或机器相关配置保存在根目录 `.env`；提交同步更新但不含真实值的 `.env.example`，不得把密钥写入脚本、文档、日志或 Git。
- 用户明确要求保存或同步到飞书时，使用飞书文档，并保存到 `纳友科技内部知识库/Agent/医疗智能体/`；同步前查看该节点的现有层级，按内容主题复用或自主创建合适的子目录。未明确要求时先保留在仓库内。

### Git 文件体积

1. 创建、下载、转换或生成文件时持续关注仓库体积；在每次 `git add` 或 `git commit` 前检查全部已修改和未跟踪文件的实际大小，重点核查 PDF、图片、音视频、压缩包、数据集、模型权重、构建产物及其他二进制文件。单文件达到 1 MiB 或相较原版本明显增大时必须评估，GitHub 的 100 MiB 硬限制不能当作可接受体积标准。
2. 只提交项目需要且适合版本管理的成品。参考项目源码、数据、模型和可再生产物放入 `references/`、`data/`、`models/`、`outputs/` 等 Git 忽略目录；需要入库的图片等资产在不损害用途的前提下压缩；不适合普通 Git 的大文件改用外部存储或 Git LFS，并在仓库中保留可复核的来源、校验和或获取说明。
3. 若无法安全压缩、无法判断文件是否应入库，或需要在压缩、忽略、Git LFS 与外部存储之间作产品取舍，必须在暂存或提交前向用户说明文件路径、大小、可选方案及影响，得到决定后再继续。提交前再次核对暂存快照，确保没有意外大文件、密钥或本地输出。

### 教程图片

1. 标签密集、要求数值或机制准确的流程图、架构图优先用 HTML/CSS/canvas 绘制并通过 Chromium 导出 PNG；封面、场景插画可使用生成式图像工具。生成式科学图是解释性插画，不能作为实验结构、系统架构或机制证据。
2. 生成式图像的 prompt 明确受众、教学目标、分区顺序、逐字标签和科学边界；参考图只作风格、构图或情绪指导时，在 prompt 中明确写明其角色，不得把它描述成待保留内容的 edit target。
3. 原始大图、候选图和测试输出保存到已被 Git 忽略的 `outputs/imagegen/` 作为本地母版，不直接移入或提交到教程目录。
4. 教程采用的图片默认从本地母版转换为 WebP `quality=90`（如 `cwebp -q 90 -m 6 -mt`），将压缩后的 `.webp` 保存到对应的 `docs/**/assets/` 并就近插入 Markdown。转换后核对尺寸、视觉伪影和压缩前后体积；不要覆盖已有资产，除非用户明确要求。
5. 生成或修改图片前先查看 `docs/style/README.md`，从索引选择最少且相关的素材。可复用的参考图、HTML/React/CSS 图稿源、prompt 和 UI 规范统一放在 `docs/style/`；供正文消费的最终图片仍放在对应文档的 `assets/` 目录，不把成品与风格素材混在一起。
6. 生成后逐张检查文字、箭头关系、事实、尺寸、空白、裁切和水印；生成式中文、数字和箭头不可靠时，用 HTML/CSS 确定性覆盖准确文字。
